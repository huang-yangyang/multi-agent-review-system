"""Core models — reserved for future use."""

from django.db import models

# No models currently defined. File upload tracking is handled
# via filesystem + RAG indexer metadata (SQLite chunks/files tables).
